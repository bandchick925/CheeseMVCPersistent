@model List<CheeseMVC.Models.CheeseCategory>
@{
    ViewData["Title"] = "Index";
}

<h2>Categories</h2>
@if (Model.Count == 0)
{
    <p>Categories Coming Soon to a Database Near You!</p>
}
<p><a asp-controller="Category" asp-action="Add">Add Category</a></p>
<div>
    <ul>
        @foreach (var cheese in Model)
        {
            <li>@cheese.Name</li>
        }
    </ul>
</div>